# 安全规范

## 提交前强制检查

- [ ] 无硬编码密钥（API key、密码、token）
- [ ] 用户输入已验证（HTTP 参数、body、文件上传）
- [ ] SQL 使用参数化查询
- [ ] HTML 输出已转义（无未净化的 `v-html` / `dangerouslySetInnerHTML`）
- [ ] 错误信息不泄露内部细节

## 密钥管理

- 密钥只通过环境变量或密钥管理服务获取
- `.env` 文件加入 `.gitignore`

## 安全事件响应

发现安全问题时立即停止当前工作，修复后再继续。
